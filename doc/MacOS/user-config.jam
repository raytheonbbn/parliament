
# This file is used to configure the Boost.Build installation.

# Toolset declarations (the first one is the default):

using clang
	: # version
	: # c++-compile-command
	:	<cxxflags>"-std=c++20 -DBOOST_LOG_WITHOUT_ASIO"
		<linkflags>"-std=c++20 -headerpad_max_install_names"
	;
